<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href='https://fonts.googleapis.com/css?family=Anta' rel='stylesheet'>
    <title>Blog</title>
    <style>
        /* General styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Anta';
            background-color: #1e6021a0;
            padding: 2px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        /* Hero banner container */
        .hero-banner-container {
            overflow: hidden;
            width: 100%;
            position: relative;
        }

        /* Hero banner */
        .hero-banner {
            display: flex;
            transition: transform 0.5s ease;
        }

        /* Individual hero slide */
        .hero-slide {
            flex: 0 0 100%;
            max-width: 100%;
            height: 100vh; /* Set the height of the banner */
            position: relative;
            background-size: cover;
            background-position: center;
        }

        .hero-slide img {
            width: 100%;
            height: 100%;
            object-fit: cover; /* Ensure the image covers the entire container */
        }

        /* Text on hero banner */
        .hero-text {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: #fff;
            text-align: center;
            font-size: 30px;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            z-index: 1;
        }

        .get-started {
            padding: 8px;
            font-size: 25px;
            margin: 25px;
            border: none;
            border-radius: 10px;
        }

        /* Media queries for mobile devices */
        @media only screen and (max-width: 768px) {
            .hero-slide {
                height: 50vh; /* Adjust the height for mobile */
            }

            .hero-text {
                font-size: 20px; /* Reduce font size for mobile */
            }

            .get-started {
                font-size: 20px; /* Reduce button font size for mobile */
            }
        }

    </style>
</head>
<body>
    <%- include('partials/navBar') %>
    <div class="hero-banner-container">
        <div class="hero-banner">
            <div class="hero-slide">
                <img src="/uploads/First.jpg" alt="Hero Slide">
                <div class="hero-text">
                    <h1>E-commerce Store Design</h1>
                    <p>Get Already Made Store With 10 Winning Product</p>
                    <button class="get-started">Get Started</button>
                    </div>
            </div>
            <div class="hero-slide">
                <img src="/uploads/second.jpg" alt="Hero Slide">
                <div class="hero-text">
                    <h1>E-commerce Marketing</h1>
                <p>Let do the work for you By get your Store To the Right Place</p>
                <button class="get-started">Get Started</button>
                </div>
            </div>
            <div class="hero-slide">
                <img src="/uploads/third.jpg" alt="Hero Slide">
                <div class="hero-text">
                    <h1>E-commerce Google Ranking</h1>
                    <p>Let Get Your Store On Google First Page</p>
                    <button class="get-started">Get Started</button>
                    </div>
            </div>
        </div>
    </div>

        <%- include('ABD/about') %>

        <div>
            <h1 style="text-align: center; margin: 20px;">Our Best Selling Gig's</h1>
            <%- include('ABD/gigs', { gigs: gigs }) %>
        </div>
        <div>
            <h2 style="text-align: center; margin: 20px;">Our Blog's</h2>
            <%- include('ABD/blog') %> 
        </div>
<footer>
    <%- include('partials/footer') %>
</footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const heroBanner = document.querySelector('.hero-banner');
            let slideIndex = 0;

            function showSlide(index) {
                heroBanner.style.transform = `translateX(-${index * 100}%)`;
            }

            function nextSlide() {
                slideIndex = (slideIndex + 1) % 3; // Assuming there are 3 slides
                showSlide(slideIndex);
            }

            // Auto slide change every 3 seconds
            setInterval(nextSlide, 5000);
        });
    </script>
</body>
</html>
