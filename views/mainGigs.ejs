<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href='https://fonts.googleapis.com/css?family=Anta' rel='stylesheet'>
    <title>Gigs Section</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body{
            font-family: 'Anta'; font-size: 22px;
            background-color: #ebebeba0;
            padding: 2px;
        }
        .hh{
            margin-top: 60px;
        }
        /* Row styles for gigs */
        .gig-row {
            margin-top: 100px;
            display: flex;
            flex-wrap: wrap;
            justify-content: space-evenly;
            gap: 20px; /* Add some space between columns */
        }

        /* Column styles for gigs */
        .gig-column {
            flex: 0 1 calc(33.33% - 20px); /* Adjust column width */
            margin-bottom: 20px; /* Add margin to the bottom of each column */
        }

        /* Card styles for gigs */
        .gig-card {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 10px;
            transition: transform 0.3s ease; /* Add a smooth transition effect */
        }

        .gig-card a{
            text-decoration: none;
            color: black;
        }

        /* Gig image styles */
        .gig-card img {
            width: 100%;
            height: 200px; /* Adjust image height */
            /* border-radius: 10px; */
            object-fit: cover; /* Ensure images maintain aspect ratio */
            margin-bottom: 15px; /* Add some space below the image */
        }

        /* Container styles for gigs */
        .gig-container {
            text-align: left;
        }

        /* Gig title styles */
        .gig-title {
            font-size: 18px;
            margin-bottom: 10px;
        }

        /* Gig description styles */
        .gig-description {
            font-size: 16px;
            color: #666;
            margin-bottom: 15px; /* Add some space below the description */
        }

        /* Button styles for gigs */
        .gig-button {
            background-color: #2a9346;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        /* Button hover styles for gigs */
        .gig-button:hover {
            background-color: #238a3d; /* Darken the background color on hover */
        }
        .checked {
        color: orange;
        }

            @media only screen and (max-width: 768px) {
        .gig-row {
            flex-direction: column; /* Stack columns vertically on smaller screens */
            align-items: center; /* Center-align columns */
            margin: 10px;
        }

        .gig-column {
            flex: 0 0 calc(100% - 20px); /* Make columns full width with some margin */
            margin-bottom: 20px; /* Add margin to the bottom of each column */
        }

        .gig-card img {
            height: auto; /* Allow image height to adjust based on content */
            margin-bottom: 10px; /* Reduce margin below the image */
        }

        .gig-title {
            font-size: 16px; /* Decrease font size for better readability */
            margin-bottom: 5px; /* Reduce margin below the title */
        }

        .gig-description {
            font-size: 14px; /* Decrease font size for better readability */
            margin-bottom: 10px; /* Reduce margin below the description */
        }

        .gig-button {
            padding: 8px 16px; /* Adjust button padding */
            font-size: 14px; /* Decrease font size for better readability */
        }
    }
    </style>
</head>
<body>

    <%- include('partials/navBar') %>
<div class="hh">
    <p style="text-align: center;">Most Popular Gig's</p>
    <section class="gig-row">
    <% if (gigs && gigs.length > 0) { %>
        <% for (var i = 0; i < gigs.length; i++) { %>
            <% var gig = gigs[i]; %>
            <div class="gig-column">
                <div class="gig-card">
                    <a href="/gigs/<%= gig._id %>">
                        <% if (gig.imageUrl) { %>
                            <img class="gig-image" src="<%= gig.imageUrl.replace('public', '') %>" alt="<%= gig.title %> image">
                        <% } %>
                        <div class="gig-container">
                            <h3 class="gig-title"><%= gig.title %></h3>
                            <p class="gig-price">Price: $<%= gig.price %></p>
                            <p style="font-size: 15px;"><span class="fa fa-star checked"></span> 5.0(5)</p>
                            <p><%= gig.category %></p>
                        </div>
                    </a>
                </div>
            </div>
        <% } %>
    <% } else { %>
        <p class="no-gigs-message">No gigs found.</p>
    <% } %> 
</section>
</div>

    <%- include('partials/footer') %>

</body>
</html>